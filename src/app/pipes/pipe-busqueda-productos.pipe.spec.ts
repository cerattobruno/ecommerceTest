import { PipeBusquedaProductosPipe } from './pipe-busqueda-productos.pipe';

describe('PipeBusquedaProductosPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeBusquedaProductosPipe();
    expect(pipe).toBeTruthy();
  });
});
